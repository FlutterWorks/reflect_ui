import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface DesignThemeProps {
}

export const DesignTheme = (props: DesignThemeProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
